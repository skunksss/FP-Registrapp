<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.drappnew">

    <!-- ✅ Permiso para acceder a internet (requerido para peticiones HTTP al backend) -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- ✅ Permiso para usar la cámara (para tomar fotos) -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- ✅ Permisos para acceso a almacenamiento (solo necesarios en versiones antiguas) -->
    <!-- Para Android 12 (API 32) e inferior -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <!-- ✅ Permisos nuevos desde Android 13 (API 33+) para acceder a medios específicos -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />

    <application
        android:name="${applicationName}"
        android:label="RegistrApp"
        android:icon="@mipmap/ic_launcher"

        <!-- ✅ Permite tráfico sin HTTPS (útil para entornos locales como 192.170.6.150) -->
        android:usesCleartextTraffic="true"

        <!-- ✅ Usa configuración de red definida en res/xml/network_security_config.xml -->
        android:networkSecurityConfig="@xml/network_security_config">

        <!-- ✅ Actividad principal de Flutter -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">

            <!-- ✅ Tema que se usará mientras se inicializa Flutter -->
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />

            <!-- ✅ Define que esta actividad se ejecuta al iniciar la app -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ✅ Requerido por Flutter para usar la nueva API de embedding -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

    <!-- ✅ Para permitir el uso del intent PROCESS_TEXT, usado por algunos plugins de Flutter -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
